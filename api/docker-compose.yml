version: "3.5"
services:
  swagger:
    image: swaggerapi/swagger-ui
    volumes:
      - ./swagger.yaml:/usr/share/nginx/html/swagger.yaml
    environment:
      API_URL: swagger.yaml
    ports:
      - "3031:8080"
